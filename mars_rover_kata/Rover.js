class Rover {
  constructor(position) {
    this.position = position;
    // this.width = width;
  }
  // receive(commands: string): void
  receive(commands) {
    return commands
  }
}


module.exports = Rover
